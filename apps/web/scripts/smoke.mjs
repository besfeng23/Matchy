const requiredRoutes = ["/", "/app", "/operator", "/admin", "/admin/finance", "/api/health"];

console.log("Matchy web smoke placeholder");
console.log(`Declared alpha routes: ${requiredRoutes.join(", ")}`);
console.log("Runtime route checks will be added after auth, database, and test server wiring are implemented.");
